import { r as registerInstance, h, i as Host } from './index-9ad756d6.js';

const imageCss = "";

const RasaImage = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.handleImgError = () => {
            this.showFallback = true;
        };
        this.src = undefined;
        this.alt = '';
        this.width = undefined;
        this.height = undefined;
        this.showFallback = false;
    }
    getDimension(value) {
        if (!value)
            return '100%';
        const numericValue = Number(value);
        if (!isNaN(numericValue)) {
            return `${numericValue}px`;
        }
        return value;
    }
    render() {
        const style = {
            width: this.getDimension(this.width),
            height: this.getDimension(this.height),
        };
        const size = typeof this.width === 'number' ? this.width : undefined;
        return (h(Host, { key: 'bd034b61724d5861b2d5753ebcca95ddfef57511' }, this.showFallback ? (h("rasa-icon-default-image-fallback", { style: style, size: size })) : (h("img", { src: this.src, alt: this.alt, onError: this.handleImgError, style: style }))));
    }
};
RasaImage.style = imageCss;

export { RasaImage as rasa_image };

//# sourceMappingURL=rasa-image.entry.js.map