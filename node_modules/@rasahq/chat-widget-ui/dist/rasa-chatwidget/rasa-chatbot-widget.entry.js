import { h, r as registerInstance, a as createEvent, g as getElement } from './index-9ad756d6.js';
import { v as v4 } from './index-1eb052fd.js';
import { M as MESSAGE_TYPES, R as Rasa } from './index-6c7a96c9.js';
import { c as configStore, D as DEBOUNCE_THRESHOLD, s as setConfigStore, a as DISCONNECT_TIMEOUT } from './config-store-b9681339.js';
import { m as messageQueueService } from './message-queue-0725e433.js';
import { w as widgetState } from './widget-state-store-1f03612e.js';
import { S as SENDER } from './constants-4997bf87.js';
import './index-d9c9f469.js';

const Messenger = ({ isOpen, isFullScreen, toggleFullScreenMode }, children) => {
    const Icon = isFullScreen ? 'rasa-icon-arrows-contract' : 'rasa-icon-arrows-expand';
    return (h("div", { class: { 'messenger': true, 'messenger--fullscreen': isFullScreen, 'messenger--open': isOpen } },
        h("div", { class: "messenger__header" },
            h("div", null,
                h("rasa-text", { value: configStore().widgetTitle, disableParsing: true })),
            configStore().toggleFullScreen && h(Icon, { onClick: toggleFullScreenMode, class: "messenger__header__icon", size: 20 })),
        h("div", { class: "messenger__content-wrapper" },
            h("div", { class: "messenger__content" }, children)),
        h("error-toast", null),
        h("rasa-chat-input", null)));
};

const isValidURL = str => {
    const urlRegex = /^(https?:\/\/)(localhost|127\.0\.0\.1|[a-zA-Z0-9.-]+)(\.[a-zA-Z]{2,})?(:\d+)?(\/[^\s]*)?$/;
    return urlRegex.test(str);
};

const broadcastChatHistoryEvent = (chatHistory, senderID) => {
    if (!senderID)
        return;
    localStorage.setItem(`rasaChatHistory-${senderID}`, chatHistory);
    localStorage.removeItem(`rasaChatHistory-${senderID}`);
};
const receiveChatHistoryEvent = (ev, callback, senderID) => {
    const newChatHistory = ev.newValue;
    if (ev.key != `rasaChatHistory-${senderID}` || !newChatHistory)
        return;
    callback(newChatHistory);
};

const SM_BREAKPOINT = 576;
const MD_BREAKPOINT = 768;
const LG_BREAKPOINT = 992;
const XL_BREAKPOINT = 1200;
const XXL_BREAKPOINT = 1920;

const isMobile = () => window.screen.width < MD_BREAKPOINT;

const debounce = (fn, ms = 300) => {
    let timeoutId;
    return function (...args) {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => fn.apply(this, args), ms);
    };
};

const rasaChatbotWidgetCss = ":root{--color-primary:#574ae2;--color-secondary:#ffffff;--color-secondary-opposite:#000000;--color-danger:#C6423F;--color-secondary-10:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 10%);--color-secondary-20:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 20%);--color-secondary-30:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 30%);--color-secondary-40:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 40%);--color-secondary-50:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 50%);--color-secondary-60:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 60%);--color-secondary-70:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 70%);--color-secondary-80:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 80%);--color-secondary-90:color-mix(in srgb, var(--color-secondary), var(--color-secondary-opposite) 90%)}:root{--font-family:\"Lato\";--launcher-background-color:var(--color-primary);--launcher-color:var(--color-secondary);--header-background-color:var(--color-primary);--header-color:var(--color-secondary);--messenger-background-color:var(--color-secondary);--input-placeholder-color:var(--color-secondary-opposite);--input-placeholder-opacity:0.5;--input-background-color:var(--color-secondary);--input-color:var(--color-secondary-opposite);--input-send-color:var(--color-primary);--message-robot-icon-background-color:var(--color-primary);--message-robot-icon-color:var(--color-secondary);--message-robot-background-color:var(--color-secondary-10);--message-robot-color:var(--color-secondary-90);--message-user-background-color:var(--color-primary);--message-user-color:var(--color-secondary);--message-button-color:var(--color-primary);--message-button-background-color:var(--color-secondary);--message-button-border-color:var(--color-primary);--message-selected-button-color:var(--color-secondary);--message-selected-button-background-color:var(--color-primary);--message-selected-button-border-color:var(--color-primary);--accordion-background-color:var(--color-secondary-10);--session-divider-color:var(--color-secondary-10);--session-divider-text-color:var(--color-secondary-40);--error-toast-background-color:var(--color-danger);--error-toast-color:var(--color-secondary);--timestamp-color:var(--color-secondary-40)}.rasa-chatbot-widget{position:fixed;bottom:20px;right:20px;z-index:99999999;--chat-message-max-width:289px;--video-height:170px}.rasa-chatbot-widget__container{position:relative}.rasa-chatbot-widget__launcher{cursor:pointer;user-select:none;display:inline-flex;justify-content:center;width:66px;height:66px;align-items:center;background:var(--launcher-background-color);border-radius:100px;color:var(--launcher-color)}.rasa-chatbot-widget__launcher-image{width:66px;height:66px;border-radius:50%}.messenger{position:absolute;justify-content:space-between;display:flex;flex-direction:column;bottom:90px;right:0;width:375px;height:85vh;max-height:700px;background:var(--messenger-background-color);border-radius:8px;box-shadow:0px 0px 8px 0px rgba(0, 0, 0, 0.05);overflow:hidden;z-index:1000;opacity:0;transform:scale(0);transform-origin:bottom right;transition:transform 300ms cubic-bezier(0, 1.2, 1, 1) 0s, opacity 83ms ease-out 0s}.messenger--open{opacity:1;transform:scale(1)}.messenger--fullscreen{position:fixed;top:0;left:0;width:100%;max-height:100%;height:calc(100% - 100px);border-radius:0}.messenger__header{display:flex;align-items:center;justify-content:space-between;color:var(--header-color);background:var(--header-background-color);padding:16px 24px;font-size:16px;line-height:124%;letter-spacing:0.6px}.messenger__header__icon{cursor:pointer}.messenger__content{padding:16px 24px 24px 24px;display:flex;flex-direction:column;gap:24px;flex:1 1 0%}.messenger__content-wrapper{height:100%;overflow:auto;display:flex;flex-direction:column-reverse}.rasa-icon{display:inline-flex}.fullscreen{--chat-message-max-width:560px;--video-width:560px;--image-width:560px}";

const RasaChatbotWidget = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.chatSessionStarted = createEvent(this, "chatSessionStarted", 7);
        this.chatWidgetReceivedMessage = createEvent(this, "chatWidgetReceivedMessage", 7);
        this.chatWidgetSentMessage = createEvent(this, "chatWidgetSentMessage", 7);
        this.chatWidgetQuickReply = createEvent(this, "chatWidgetQuickReply", 7);
        this.chatWidgetOpened = createEvent(this, "chatWidgetOpened", 7);
        this.chatWidgetClosed = createEvent(this, "chatWidgetClosed", 7);
        this.chatWidgetHyperlinkClicked = createEvent(this, "chatWidgetHyperlinkClicked", 7);
        this.chatWidgetFileStartedDownload = createEvent(this, "chatWidgetFileStartedDownload", 7);
        this.messageDelayQueue = Promise.resolve();
        this.disconnectTimeout = null;
        this.sentMessage = false;
        this.sessionConfirm = () => {
            this.chatSessionStarted.emit({ sessionId: this.client.sessionId });
        };
        this.onNewMessage = (data) => {
            // If senderID is configured (continuous session), tab is not in focus and user message was not sent from this tab do not render new server message
            if (this.senderId && !document.hasFocus() && !this.sentMessage)
                return;
            this.chatWidgetReceivedMessage.emit(data);
            const delay = data.type === MESSAGE_TYPES.SESSION_DIVIDER || data.sender === SENDER.USER ? 0 : configStore().messageDelay;
            this.messageDelayQueue = this.messageDelayQueue.then(() => {
                return new Promise(resolve => {
                    this.typingIndicator = delay > 0;
                    setTimeout(() => {
                        messageQueueService.enqueueMessage(data);
                        this.typingIndicator = false;
                        // If senderID is configured and message was sent from this tab, broadcast event to share chat history with other tabs with same senderID 
                        if (this.senderId && this.sentMessage) {
                            debounce(() => {
                                broadcastChatHistoryEvent(this.client.getChatHistory(), this.senderId);
                                this.sentMessage = false;
                            }, DEBOUNCE_THRESHOLD)();
                        }
                        resolve();
                    }, delay);
                });
            });
        };
        this.loadHistory = (data) => {
            this.messages = data;
        };
        this.toggleOpenState = () => {
            const nextValue = !this.isOpen;
            this.isOpen = nextValue;
            this.client.reconnection(nextValue);
            clearTimeout(this.disconnectTimeout);
            this.disconnectTimeout = null;
            this.isOpen ? this.connect() : this.disconnect();
            this.emitChatWidgetOpenCloseEvents();
        };
        this.toggleFullscreenMode = () => {
            this.isFullScreen = !this.isFullScreen;
        };
        this.isOpen = false;
        this.isFullScreen = isMobile();
        this.messageHistory = [];
        this.messages = [];
        this.typingIndicator = false;
        this.cachedMessages = [];
        this.isConnected = false;
        this.serverUrl = undefined;
        this.authenticationToken = '';
        this.widgetTitle = 'Rasa Widget';
        this.botIcon = '';
        this.widgetIcon = '';
        this.displayTimestamp = false;
        this.messageTimestamp = '';
        this.initialPayload = '';
        this.senderId = '';
        this.messageDelay = 600;
        this.streamMessages = false;
        this.autoOpen = false;
        this.errorMessage = 'Something bad happened';
        this.toggleFullScreen = false;
        this.inputMessagePlaceholder = 'Type your message here';
        this.restEnabled = false;
    }
    componentWillLoad() {
        const { serverUrl, authenticationToken, widgetTitle, botIcon, widgetIcon, displayTimestamp, messageTimestamp, initialPayload, senderId, messageDelay, streamMessages, autoOpen, errorMessage, toggleFullScreen, inputMessagePlaceholder, restEnabled, } = this;
        setConfigStore({
            serverUrl,
            authenticationToken,
            widgetTitle,
            botIcon,
            widgetIcon,
            displayTimestamp,
            messageTimestamp,
            initialPayload,
            senderId,
            streamMessages,
            messageDelay: streamMessages ? 0 : messageDelay,
            autoOpen,
            errorMessage,
            toggleFullScreen,
            inputMessagePlaceholder,
            restEnabled,
        });
        const protocol = this.restEnabled ? 'http' : 'ws';
        this.client = new Rasa({ url: this.serverUrl, protocol, initialPayload, authenticationToken, senderId });
        this.client.on('connect', () => {
            this.isConnected = true;
            // widgetState connected needed for enabling input
            widgetState.getState().state.connected = true;
        });
        this.client.on('message', this.onNewMessage);
        this.client.on('loadHistory', this.loadHistory);
        this.client.on('sessionConfirm', this.sessionConfirm);
        this.client.on('disconnect', () => {
            this.isConnected = false;
            // widgetState connected needed for disabling input
            widgetState.getState().state.connected = false;
        });
        if (this.autoOpen) {
            this.toggleOpenState();
        }
        // If senderID is configured watch for storage change event (localStorage) and override chat history (sessionStorage)
        // This happens on tabs that are not in focus nor message was sent from that tab
        if (this.senderId) {
            window.onstorage = ev => {
                receiveChatHistoryEvent(ev, this.client.overrideChatHistory, this.senderId);
            };
        }
    }
    scrollToBottom() {
        const container = this.el.shadowRoot.querySelector('.messenger__content-wrapper');
        if (container) {
            container.scrollTop = 0;
        }
    }
    connect() {
        if (this.isConnected)
            return;
        this.client.connect();
    }
    disconnect() {
        if (!this.isConnected)
            return;
        this.disconnectTimeout = setTimeout(() => {
            if (!this.isOpen) {
                this.client.disconnect();
                this.messageHistory = [];
                this.messages = [];
            }
        }, DISCONNECT_TIMEOUT);
    }
    emitChatWidgetOpenCloseEvents() {
        this.isOpen ? this.chatWidgetOpened.emit() : this.chatWidgetClosed.emit();
    }
    connectedCallback() {
        messageQueueService.getState().onChange('messageToRender', message => {
            this.messages = [...this.messages, message];
        });
    }
    sendMessageHandler(event) {
        const timestamp = new Date();
        this.client.sendMessage({ text: event.detail, timestamp });
        this.chatWidgetSentMessage.emit(event.detail);
        this.messages = [...this.messages, { type: 'text', text: event.detail, sender: 'user', timestamp }];
        this.scrollToBottom();
        this.sentMessage = true;
    }
    quickReplySelected({ detail: { quickReply, key } }) {
        const timestamp = new Date();
        this.messages = [...this.messages, { type: 'text', text: quickReply.text, sender: 'user', timestamp }];
        const updatedMessage = this.messages[key];
        updatedMessage.replies.find(qr => qr.reply === quickReply.reply).isSelected = true;
        this.messages[key] = updatedMessage;
        this.client.sendMessage({ text: quickReply.text, reply: quickReply.reply, timestamp }, true, key - 1);
        this.chatWidgetQuickReply.emit(quickReply.reply);
        this.sentMessage = true;
    }
    linkClickedHanlder() {
        this.chatWidgetHyperlinkClicked.emit();
    }
    linkClickedHanlder() {
        this.chatWidgetFileStartedDownload.emit();
    }
    getAltText() {
        return this.isOpen ? 'Close Chat' : 'Open Chat';
    }
    onMessagesChange() {
        if (this.cachedMessages.length !== this.messages.length) {
            this.cachedMessages = this.messages.map((message, key) => this.renderMessage(message, false, key));
        }
    }
    renderMessage(message, isHistory = false, key) {
        switch (message.type) {
            case MESSAGE_TYPES.SESSION_DIVIDER:
                return h("rasa-session-divider", { sessionStartDate: message.startDate, key: key });
            case MESSAGE_TYPES.TEXT:
                return (h("chat-message", { sender: message.sender, key: key, timestamp: message.timestamp }, h("rasa-text-message", { sender: message.sender, value: message.text, isHistory: isHistory })));
            case MESSAGE_TYPES.IMAGE:
                return (h("chat-message", { sender: message.sender, key: key, timestamp: message.timestamp }, h("rasa-image-message", { imageSrc: message.imageSrc, text: message.text, imageAlt: message.alt })));
            case MESSAGE_TYPES.VIDEO:
                return (h("chat-message", { sender: message.sender, key: key, timestamp: message.timestamp }, h("rasa-video", { src: message.src })));
            case MESSAGE_TYPES.FILE_DOWNLOAD:
                return (h("chat-message", { sender: message.sender, key: key, timestamp: message.timestamp }, h("rasa-file-download-message", { fileUrl: message.fileUrl, fileName: message.fileName, text: message.text })));
            case MESSAGE_TYPES.ACCORDION:
                return (h("chat-message", { sender: message.sender, key: key, timestamp: message.timestamp }, message.elements.map(element => (h("rasa-accordion", { label: element.title }, h("rasa-text", { value: element.text }))))));
            case MESSAGE_TYPES.QUICK_REPLY:
                let activeQuickReplyId = '';
                if (!isHistory) {
                    activeQuickReplyId = v4();
                    widgetState.getState().state.activeQuickReply = activeQuickReplyId;
                }
                return h("rasa-quick-reply", { quickReplyId: activeQuickReplyId, message: message, elementKey: key, key: key, isHistory: isHistory });
            case MESSAGE_TYPES.CAROUSEL:
                return (h("chat-message", { sender: message.sender, timestamp: message.timestamp }, h("rasa-carousel", { elements: message.elements })));
            case MESSAGE_TYPES.RATING:
                return (h("chat-message", { sender: message.sender, key: key, timestamp: message.timestamp }, h("rasa-rating", { text: message.text, options: JSON.stringify(message.options), message: message.message, onRatingSelected: (event) => {
                        console.log("Rating selected:", event.detail.value);
                        console.log("Payload triggered:", event.detail.payload);
                    } })));
        }
    }
    render() {
        if (!isValidURL(this.serverUrl)) {
            console.error("Widget misconfigured. Missing property 'serverUrl'");
            return null;
        }
        const widgetClassList = {
            'rasa-chatbot-widget': true,
            'fullscreen': this.isFullScreen,
        };
        return (h("global-error-handler", null, h("slot", null), h("div", { class: widgetClassList }, h("div", { class: "rasa-chatbot-widget__container" }, h(Messenger, { isOpen: this.isOpen, toggleFullScreenMode: this.toggleFullscreenMode, isFullScreen: this.isFullScreen }, this.messageHistory.map((message, key) => this.renderMessage(message, true, key)), this.cachedMessages, this.typingIndicator && h("rasa-typing-indicator", null)), h("div", { role: "button", onClick: this.toggleOpenState, class: "rasa-chatbot-widget__launcher", "aria-label": this.getAltText() }, configStore().widgetIcon ? (h("img", { src: configStore().widgetIcon, class: "rasa-chatbot-widget__launcher-image" })) : this.isOpen ? (h("rasa-icon-close-chat", { size: 18 })) : (h("rasa-icon-chat", null)))))));
    }
    get el() { return getElement(this); }
    static get watchers() { return {
        "messages": ["onMessagesChange"]
    }; }
};
RasaChatbotWidget.style = rasaChatbotWidgetCss;

export { RasaChatbotWidget as rasa_chatbot_widget };

//# sourceMappingURL=rasa-chatbot-widget.entry.js.map