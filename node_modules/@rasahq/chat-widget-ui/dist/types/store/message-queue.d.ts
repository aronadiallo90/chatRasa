import { Message } from '@rasahq/chat-widget-sdk';
export declare const messageQueueService: {
    enqueueMessage: (message: Message) => void;
    getState: () => {
        state: {
            messageQueue: Message[];
            isRendering: boolean;
            messageToRender: Message | null;
        };
        onChange: import("@stencil/store/dist/types").OnChangeHandler<{
            messageQueue: Message[];
            isRendering: boolean;
            messageToRender: Message | null;
        }>;
    };
    completeRendering: () => void;
};
