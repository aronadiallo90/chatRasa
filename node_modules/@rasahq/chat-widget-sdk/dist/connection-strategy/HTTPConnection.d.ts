import { ConnectionParams, ConnectionStrategy } from './ConnectionStrategy';
export declare class HTTPConnection implements ConnectionStrategy {
    url: string;
    authenticationToken?: string;
    onConnect: () => void;
    onDisconnect: () => void;
    onBotResponse: (data: unknown) => void;
    onSessionConfirm: () => void;
    constructor(options: ConnectionParams);
    connect(): void;
    private normalizeResponse;
    sendMessage(message: string, sessionId: string): Promise<void>;
    disconnect(): void;
    sessionRequest(_sessionId: string): void;
    reconnection(): void;
}
