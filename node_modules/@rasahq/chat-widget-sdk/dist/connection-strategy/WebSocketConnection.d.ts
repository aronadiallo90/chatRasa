import { Socket } from 'socket.io-client';
import { ConnectionParams, ConnectionStrategy } from './ConnectionStrategy';
export declare class WebSocketConnection implements ConnectionStrategy {
    url: string;
    authenticationToken?: string;
    socket: Socket;
    onConnect: (isReconnect?: boolean) => void;
    onDisconnect: () => void;
    onBotResponse: (data: unknown) => void;
    onSessionConfirm: () => void;
    private isReconnecting;
    constructor(options: ConnectionParams);
    connect(): void;
    sendMessage(message: string, sessionId: string): void;
    disconnect(): void;
    sessionRequest(sessionId: string): void;
    reconnection(value: boolean): void;
    initEvents(): void;
}
