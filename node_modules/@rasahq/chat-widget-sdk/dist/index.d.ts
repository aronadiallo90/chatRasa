import { EventEmitter } from './EventEmitter';
interface Options {
    url: string;
    protocol?: 'http' | 'ws';
    initialPayload?: string;
    authenticationToken?: string;
    senderId?: string;
}
export declare class Rasa extends EventEmitter {
    private _sessionId;
    private storageService;
    private connection;
    private initialPayload;
    private isInitialConnection;
    private isSessionConfirmed;
    private senderId?;
    constructor({ url, protocol, initialPayload, authenticationToken, senderId }: Options);
    get sessionId(): string;
    private set sessionId(value);
    private loadChatHistory;
    private onBotResponse;
    private onSessionConfirm;
    private onConnect;
    private onDisconnect;
    connect(): void;
    disconnect(): void;
    sendMessage({ text, reply, timestamp }: {
        text: string;
        reply?: string;
        timestamp?: Date;
    }, isQuickReply?: boolean, messageKey?: number): void;
    reconnection(value: boolean): void;
    overrideChatHistory: (chatHistoryString: string) => void;
    getChatHistory(): string;
}
export * from './errors';
export * from './message-parser';
export * from './constants';
